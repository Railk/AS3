package railk.as3.effect.sticker 
{
	public class DistancePeeloutDetector implements IPeeloutDetector 
	{
		public function DistancePeeloutDetector() {
		}

		public function detect(sticker:StickerBase):Boolean {
			return false;
		}
	}
}
